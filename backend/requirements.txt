fastapi==0.139.0
uvicorn==0.51.0
python-dotenv==1.2.2
pydantic==2.13.4
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-jose[cryptography]==3.3.0
python-multipart==0.0.9
email-validator==2.2.0
boto3==1.35.74
slowapi==0.1.10
# Talks to Razorpay's REST API directly (see app/services/payments.py)
# instead of the `razorpay` SDK — that package's client.py does
# `import pkg_resources` (from setuptools), which recent Python builds
# (3.12+, including Render's) no longer ship by default, so the app
# failed to boot with "ModuleNotFoundError: No module named
# 'pkg_resources'". requests + hmac/hashlib (stdlib) cover everything we
# actually need from Razorpay: creating an order and verifying a payment
# signature.
requests==2.32.3